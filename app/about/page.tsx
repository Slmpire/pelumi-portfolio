import { createMetadata } from "@/lib/metadata"

export const metadata = createMetadata({
  title: "About — Pelumi Ogunleye",
  description: "Engineer by training, builder by choice. EEE student at OAU Ife and full-stack developer.",
  path: "/about",
})

export default function AboutPage() {
  return <div />
}